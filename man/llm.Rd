% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{llm}
\alias{llm}
\title{Create an ellmer LLM client backed by Ollama}
\usage{
llm(
  model = "gemma3:4b",
  system_prompt =
    paste("You extract structured fields from epidemiological case investigation reports.",
    "The most important details are laboratory result, notification, interview timing, symptoms, exposures, travel, contacts, and residence.",
    "Only return values grounded in the supplied text.",
    "Use missing values when information is absent."),
  base_url = Sys.getenv("OLLAMA_BASE_URL", "http://localhost:11434"),
  params = ellmer::params(temperature = 0),
  check_connection = TRUE,
  pull = FALSE,
  ...
)
}
\arguments{
\item{model}{Ollama model name.}

\item{system_prompt}{System prompt sent to the model.}

\item{base_url}{Ollama server URL.}

\item{params}{Model generation parameters passed to \code{ellmer::chat_ollama()}.
Defaults to \code{ellmer::params(temperature = 0)} for more stable extraction.}

\item{check_connection}{Should the local Ollama server be checked first?}

\item{pull}{Should the model be pulled with \code{ollamar::pull()} before use?}

\item{...}{Additional arguments passed to \code{ellmer::chat_ollama()}.}
}
\value{
An LLM client object suitable for \code{ellmer::parallel_chat_structured()}.
}
\description{
Uses \code{ollamar} to optionally verify that Ollama is running and to pull a model
before returning an \code{ellmer::chat_ollama()} client object.
}
